import React, { useContext } from 'react';

function Search(){
  return(
    <form action="">
      <input type="text" placeholder="search"/>
      <button>Find</button>
    </form>
  )
}
export default Search;